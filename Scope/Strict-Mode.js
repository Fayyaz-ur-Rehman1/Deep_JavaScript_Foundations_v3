// the statement "use strict" ; instruct the broswer to use the strit mode , which is a reduced  and safer feature set of javascript

"use strict"

function sum(a, b) {
    add = a + b
    console.log(add);
}
sum(20, 10)

function sum1(a, a) {
    var add = a + 20
    console.log(add);
}
sum1(10, 30)